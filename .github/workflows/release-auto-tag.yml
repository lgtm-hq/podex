---
name: Release - Auto Tag

"on":
  push:
    branches: [main]
  workflow_dispatch:

permissions: {}

concurrency:
  group: release-auto-tag-${{ github.repository }}
  cancel-in-progress: false

jobs:
  auto-tag:
    # yamllint disable-line rule:line-length
    uses: lgtm-hq/lgtm-ci/.github/workflows/reusable-release-auto-tag.yml@a167ee7165319d00dc92604ecdef87b719a0a55d  # v0.74.4
    with:
      # Default version-source (commit) reads the version from the merged
      # `chore(release): version X.Y.Z` commit — language-agnostic, so no
      # version-source/version-file needed for the backend/ monorepo layout.
      # Podex ships GitHub Releases (Docker images to GHCR on tag), not PyPI.
      create-release: true
      tooling-ref: 'a167ee7165319d00dc92604ecdef87b719a0a55d' # v0.74.4
      egress-policy: block
      egress-preset: github-tooling
    secrets:
      RELEASE_APP_ID: ${{ secrets.RELEASE_APP_ID }}
      RELEASE_APP_PRIVATE_KEY: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}
    permissions:
      actions: read
      contents: write
      issues: write
