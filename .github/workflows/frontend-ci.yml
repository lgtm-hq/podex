---
name: CI - Frontend

"on":
  push:
    branches: [main]
    paths: ['frontend/**', '.github/workflows/frontend-ci.yml']
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]
    paths: ['frontend/**', '.github/workflows/frontend-ci.yml']
  merge_group:
  workflow_dispatch:

permissions: {}

concurrency:
  group: frontend-ci-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  frontend:
    # yamllint disable-line rule:line-length
    uses: lgtm-hq/lgtm-ci/.github/workflows/reusable-test-node-custom.yml@6a96470c698addc35f81b5420e1e4114b35c5a03  # v0.59.1
    with:
      package-manager: bun
      working-directory: frontend
      # astro check then vitest with coverage (threshold in vitest.config.ts);
      # the && short-circuits so a check failure also fails the job.
      test-command: 'bun run check && bun run test:coverage'
      coverage: true
      job-name: Frontend Checks
      node-version: '22'
      draft-pr-skip: true
      tooling-ref: '6a96470c698addc35f81b5420e1e4114b35c5a03'  # v0.59.1
      # audit (not block) for now: no ready egress preset covers the node/bun
      # install registries. Tighten to block with an explicit allowlist later.
      egress-policy: audit
    permissions:
      actions: write
      contents: read
      pull-requests: write
