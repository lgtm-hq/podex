# Podex Backend

FastAPI backend for the Podex podcast media index.

## Quick Start

```bash
uv sync
uv run python scripts/seed_mock_data.py
uv run uvicorn podex.main:app --reload
```

API docs at <http://localhost:8000/docs>
